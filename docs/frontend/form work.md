Описание работы кнопок формы

Всего в форме есть 4 кнопки. 2 для работы со всем массивом и 2 для работы с объектом. 

Кнопки для работы с массивом

Очистить
Очищает весь массив. Удаляет часть  dom с массивом и вставляет на его место чистый массив созданный заново с помощью createForm @services/web_app/static/create_form.js. У нажатой кнопки есть свойство data-parent в котором записан уникальный ключ id элемента формы (массива) который нужно заменить в dom дереве. Он же является путем к этому элементу в шаблоне для функции createForm. Нужно создать новый элемент массива со всем содержимым и вставить его в dom вместо старого массива.

Добавить
Добавляет новый объект в конец массива.
Свойство data-target кнопки содержит id контейнера, содержащего все объекты массива в форме. Нужно посчитать их количество, пусть это X. И создать чистый элемент. Для этого расчитать новый путь: к data-target добавить "_" X+1. Полученный путь передать в createForm и смонтировать полученый результат последним в контейнер data-target.

Кнопки для работы с объектом

Очистить
Удаляет содержимое объекта. Для этого удаляет и создает новый объект массива в замен старого. Свойство data-path кнопки - это путь к объекту массива. Создаем чистый объект, передавая путь в createForm. Полученный код монтируем в dom вместо очищаемего.

Удалить




Использование функции createForm @docs/frontend/create_form/create_form.md
Скрипт создать в файле form_work.js